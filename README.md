# SPORTIK

Минималистичное приложение для домашних тренировок с гантелями и скамьёй: статичные иллюстрации техники, недельный план, прогресс, персональный AI‑тренер и синхронизация между устройствами.

## Что уже работает

- 10 упражнений со статичными иллюстрациями техники;
- пошаговая тренировка с фиксацией каждого подхода;
- выбор веса одной гантели и их количества (по умолчанию 2 × 5 кг);
- создание и реальное обновление плана и календаря через AI‑тренера;
- голосовые сообщения: запись до 5 минут, пауза без отправки и серверная расшифровка;
- пустое стартовое состояние без демонстрационной статистики и готового плана;
- профиль, статистика и хранение прогресса в браузере;
- подготовленная авторизация Supabase по e-mail и Google;
- облачная синхронизация одного прогресса на компьютере и телефоне;
- защищённый серверный endpoint с выбором Gemini или OpenAI;
- PWA: приложение можно добавить на домашний экран телефона.

Без серверного ключа Gemini или OpenAI приложение хранит данные и фиксирует тренировки, но персональное создание плана через AI недоступно.

## Рекомендуемая архитектура

- **Vercel** — публичный сайт и защищённая функция `/api/coach`;
- **Supabase** — вход, база данных и Row Level Security;
- **Gemini API или OpenAI Responses API** — персональный тренер;
- **GitHub** — публичный исходный код и автоматическое развёртывание в Vercel.

GitHub Pages не подходит для AI‑части: на чисто статическом сайте нельзя безопасно хранить `OPENAI_API_KEY`.

## Локальный просмотр без облака

Откройте `index.html` либо запустите сервер из этой папки:

```bash
python3 -m http.server 4173
```

Затем откройте `http://127.0.0.1:4173`.

## 1. Подключение Supabase

1. Создайте бесплатный проект на [supabase.com](https://supabase.com/).
2. Откройте **SQL Editor** и выполните файл [`supabase/schema.sql`](supabase/schema.sql).
3. В **Authentication → URL Configuration** добавьте локальный и будущий Vercel URL.
4. Вход по e-mail работает после настройки Site URL. Google можно дополнительно включить в **Authentication → Providers → Google**.
5. Скопируйте Project URL и anon/publishable key. Это публичные клиентские параметры; безопасность данных обеспечивается RLS из `schema.sql`.

## 2. Бесплатный старт с Gemini

1. Создайте API-ключ в [Google AI Studio](https://aistudio.google.com/apikey).
2. Никогда не вставляйте ключ в `app.js`, браузер или публичный репозиторий.
3. Добавьте в Vercel серверные переменные:

```text
AI_PROVIDER=gemini
GEMINI_API_KEY=ваш_ключ
GEMINI_MODEL=gemini-3.5-flash
GEMINI_TRANSCRIBE_MODEL=gemini-3.5-flash
```

4. Выполните Redeploy. Один и тот же Gemini-провайдер будет использоваться для диалога и резервной расшифровки голоса.

На бесплатном тарифе Google может использовать отправленные данные для улучшения продуктов. Не сообщайте тренеру лишние медицинские или иные чувствительные данные. Лимиты Gemini зависят от проекта и могут меняться.

### Возврат к OpenAI

1. Создайте отдельный project API key на [странице API keys](https://platform.openai.com/api-keys).
2. Никогда не вставляйте ключ в `app.js`, браузер или публичный GitHub‑репозиторий.
3. Установите в Vercel `AI_PROVIDER=openai` и храните ключ как серверную переменную `OPENAI_API_KEY`.
4. Рекомендуемый экономичный стартовый вариант — `gpt-5.6-luna`; модель можно изменить переменной `OPENAI_MODEL`.
5. Настройте месячный бюджет и уведомления об использовании в OpenAI Platform.

Локальный шаблон переменных находится в `.env.example`. Реальный `.env.local` исключён из Git.

## 3. Публикация в Vercel

1. Импортируйте GitHub‑репозиторий в [Vercel](https://vercel.com/new).
2. Framework Preset: **Other**; корневая папка — корень репозитория.
3. Добавьте переменные:

```text
AI_PROVIDER=gemini
GEMINI_API_KEY
GEMINI_MODEL=gemini-3.5-flash
GEMINI_TRANSCRIBE_MODEL=gemini-3.5-flash
SUPABASE_URL
SUPABASE_ANON_KEY
```

4. Нажмите Deploy.
5. Полученный `https://....vercel.app` добавьте в Supabase Authentication URL Configuration.
6. На телефоне откройте URL и выберите «Добавить на экран Домой».

## Безопасность

- `/api/coach` работает только после входа, применяет ограничение частоты и не раскрывает серверный ключ или ваш API-баланс посторонним;
- Gemini/OpenAI key остаётся только на сервере;
- RLS разрешает пользователю читать и менять только собственный прогресс;
- сообщения ограничены по длине, а endpoint имеет базовое ограничение частоты;
- медицинские рекомендации ограничены безопасными рамками и не заменяют врача.

## Структура

```text
api/coach.js           защищённый SPORTIK Coach
api/transcribe.js      защищённая расшифровка голосовых сообщений
api/config.js          публичная конфигурация Supabase
assets/exercises/      изображения упражнений
supabase/schema.sql    таблица прогресса и RLS
app.js                 интерфейс, тренировки, auth и sync
styles.css             дизайн и статичные изображения техники
manifest.webmanifest   установка на телефон
service-worker.js      офлайн-кэш
```

## Следующие производственные шаги

- добавить журнал веса/повторов для каждого подхода;
- включить серверное суточное ограничение AI‑запросов;
- добавить экспорт и удаление аккаунта;
- провести проверку программы тренировок с квалифицированным тренером.
